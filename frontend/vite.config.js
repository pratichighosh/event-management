import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/', // Ensures correct path resolution for deployment
  build: {
    outDir: 'dist', // Explicitly specify output directory
    rollupOptions: {
      output: {
        manualChunks(id) {
          if (id.includes('node_modules')) {
            return 'vendor';
          }
        }
      }
    }
  },
  server: {
    port: 3000, // Optional: specify development server port
    open: true  // Optional: automatically open browser
  }
})